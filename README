To enable this ebuild repository on your Gentoo system:

	# emerge -n app-eselect/eselect-repository dev-vcs/git
	# eselect repository enable bitcoin
	# emaint sync -r bitcoin

Come to freenode #bitcoin-gentoo and indicate your GitLab account name if you would like commit access.
